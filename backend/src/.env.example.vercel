# --- Vercel Environment Variables ---
# These variables should be set in your Vercel project settings.

# Database Credentials
DB_USER="sa"
DB_PASSWORD=""
DB_SERVER="109.73.161.154"
DB_PORT="55572"
DB_DATABASE="KSDC_SMART_LIVE_STAGING"

# API Key for Backend (process.env.API_KEY)
# Generate a strong, random key for production.
API_KEY="YOUR_SECURE_PRODUCTION_API_KEY"

# API Key for Frontend (import.meta.env.VITE_API_KEY)
# Use the SAME secure key as API_KEY.
VITE_API_KEY="YOUR_SECURE_PRODUCTION_API_KEY"

# Allowed Databases (comma-separated)
ALLOWED_DATABASES="KSDC_SMART_LIVE_STAGING,KSDC_SMART_LIVE"

# Node Environment (set to "production" for live deployments)
NODE_ENV="production"